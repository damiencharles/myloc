{% extends 'base.html.twig' %}

{% block title %} Profil de {{user.pseudo}} {% endblock %}

{% block body %}
<div class="container">
    <h2 class="text-center my-5">Profil de {{user.pseudo}}</h2>
        <div class="row text-center d-flex justify-content-center align-items-center my-4 description">  

            <div class='col-6 my-3'>
                <div><i class="fas fa-user-circle fa-5x mt-2"></i></div>
                <div class="mt-3">Pseudo : {{user.pseudo}}</div>
                {% if user.points >= 200 %}
                <div class="mt-3"> Super Prêteur <i class="fas fa-medal"></i></div>
                {% endif %}
            </div>

            <div class='col-6 my-3'>
                <div><i class="fas fa-envelope mr-2"></i>Email : {{user.email}}</div>
                <div class="mt-2"><i class="fas fa-star mr-2"></i>Ses points : {{user.points}} points</div>            
            </div>
    <hr>
        </div>

{% for bien in biens %}
{{bien.nomBien}}
{% endfor %}
<br>
</div>


{% endblock %}
